<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- Android 15 (API 35) forces edge-to-edge, and the WebView doesn't report
             CSS safe-area insets reliably, so content slid under the status bar +
             camera cutout. Opt out so the bar reserves space and content lays out
             below it. (Documented escape hatch for apps not yet handling insets.) -->
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
        <!-- Force the window + system bars to the dark field colour regardless of the
             phone's light/dark setting, so the status-bar inset area isn't a white band
             (DayNight light mode was painting it white). Light icons on the dark bar. -->
        <item name="android:windowBackground">#FF0A0F1C</item>
        <item name="android:statusBarColor">#FF0A0F1C</item>
        <item name="android:navigationBarColor">#FF0A0F1C</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>